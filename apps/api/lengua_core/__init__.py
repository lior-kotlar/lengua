"""Pure, portable domain logic (no FastAPI, no SQL).

Ported from the legacy ``lengua/`` package. Kept import-clean so it stays unit-testable
and reusable across the HTTP layer, scripts, and tests.
"""
