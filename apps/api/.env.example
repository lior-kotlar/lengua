# Copy to apps/api/.env and adjust. All values below are safe defaults for local dev.

# LLM provider seam. Defaults to Groq (free tier) for all dev/CI; flip to "gemini" later.
LLM_PROVIDER=groq
LLM_MODEL=llama-3.1-8b-instant
