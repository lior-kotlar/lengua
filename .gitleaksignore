# gitleaks accepted-findings allowlist (by fingerprint).
#
# These two fingerprints are FAKE secrets that were committed to THIS PR's branch
# (ci/phase0-quality-gate) solely to prove the gitleaks gate fails on a planted secret
# (Phase 0 task 0.5.7) — see commit c8b3bdd "PROOF(throwaway)…". The file was deleted in a
# later commit, but gitleaks scans the PR's whole commit *range* from the base, so it still
# flags the historical add. These are non-real, non-functional placeholder strings.
#
# This allowlist is intentionally scoped to the two EXACT commit-pinned fingerprints — it does
# NOT broaden the ruleset: any new/other secret still fails the gate. The pinned commits do not
# exist on `main` after this PR is squash-merged, so this file becomes inert there and is
# removed in the follow-up no-op PR (0.5.10).
c8b3bdd907a818d62b86fe9536c774adeb3c1fe5:PROOF_SECRET_DELETEME.txt:generic-api-key:4
c8b3bdd907a818d62b86fe9536c774adeb3c1fe5:PROOF_SECRET_DELETEME.txt:generic-api-key:5
