-- Local dev seed data.
-- Applied automatically by `supabase db reset`.
-- Add test users, sample languages, or reviewer accounts here when needed.

-- Example: insert a dev test user (Supabase auth users are created via the Auth API,
-- not raw SQL, so seed data here is for app tables only after auth.users exists).
