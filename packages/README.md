# packages/

Shared code consumed by more than one app.

- `api-types/` — OpenAPI-generated TypeScript client/types, generated from the FastAPI
  OpenAPI schema and consumed by `apps/web`. Created in Phase 1 once the API exposes its
  schema.
