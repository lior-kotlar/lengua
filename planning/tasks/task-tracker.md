# Lengua Productionization — Master Task Tracker

This is the **master index** for the Lengua productionization plan (Streamlit → FastAPI +
React + Supabase + Cloud Run, packaged to iOS/Android via Capacitor). The granular,
PR-sized tasks live in the sibling `phase-N-*.md` files in this directory; you tick the
`- [ ]` checkboxes **there**, and this file rolls them up into a single phase-level view —
a summary table, the dependency graph and critical path, the cross-cutting workstreams,
setup readiness, and the launch milestones. Start here to orient, then open the relevant
phase file to do the work.

---

## How to use this tracker

- **Work phases in dependency order.** Honor the `Depends on` column and the dependency
  graph below — a phase isn't startable until its prerequisites are met.
- **Within a phase, the groups are mostly parallel.** Each phase file is split into
  numbered groups (e.g. `1.1`, `1.2`); unless a task lists an explicit `depends:`, groups
  can progress in parallel.
- **Each `- [ ]` is one PR-sized, independently verifiable task.** Every task carries a
  `verify:` line — the concrete check that proves it's done.
- **Tick a box only when its `verify` passes** and the PR lands green under the per-PR
  quality gate (100% green + ≥80% backend & frontend coverage + Playwright E2E).
- **Optionally promote a task to a GitHub issue when you start it** (and link the PR), so
  in-flight work is visible. Update the phase Status here as a phase moves.

## Status legend

`[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Phase summary

| Phase | File | Focus | Effort | Depends on | Tasks | Status |
|------:|------|-------|:------:|------------|:-----:|--------|
| 0 | [phase-0-foundations.md](phase-0-foundations.md) | Monorepo, tooling, CI quality gate, accounts | S–M | — | 45 | **impl complete** — 0.1–0.5 (CI gate 0.5.1–0.5.10), 0.6.1–0.6.2, 0.7.1–0.7.6/0.7.9/0.7.10/0.7.11, 0.8.x all merged green; gate live + green on `main`. Remaining = OWNER-only (non-blocking): 0.6.3/0.6.4 deferred to launch, 0.7.7/0.7.8 open. Does not block Phase 1. |
| 1 | [phase-1-backend-core.md](phase-1-backend-core.md) | FastAPI + Postgres core loop, LLM seam, OpenAPI | L | 0 | 34 | **impl complete** — all 34 tasks merged green + every exit-gate item verified. 1.1 done (lengua_core ported to a pure core: scheduler/proficiency/cards/config; SQLite relocated to legacy_streamlit/store; legacy app still runnable). 1.2 done (LLM provider seam: Groq default + Gemini reserved behind one Protocol, `get_provider()` fail-fast, shared retry/backoff + request caps; flip via `LLM_PROVIDER`). 1.3 done — 1.3a (async SQLAlchemy 2.0 engine/sessionmaker + `get_db` in `app/db/`, typed ORM models for all 8 tables matching the canonical schema, async `db_session` fixture) + 1.3b (`app/repositories/` = the only DB-touching layer, every method `user_id`-scoped; `app/services/` orchestrates pure `lengua_core` + repos with zero raw SQL — Generate→Save / Review-grade / Discover / languages / proficiency / settings). 1.4 done — Alembic at `apps/api/migrations/` (async `env.py` → `Base.metadata`, URL from `DATABASE_URL`); first migration = the full schema (6 app tables + `llm_usage`/`llm_budget`, applyable on bare Postgres, `alembic check` clean, upgrade↔downgrade round-trip); idempotent fixed-UUID dev-user seed (`scripts/seed_dev_user.py`, bare-DB insert + Supabase auth-backed). 1.5 in progress — 1.5a done (FastAPI app + `app/deps.py` `get_db`/`current_user`(seeded dev UUID)/`get_llm_provider`; core-loop routers wired into `create_app`: `GET/POST/PATCH/DELETE /languages`, `POST /generate`, `POST /cards/save`, `GET /review/due` split + `POST /review/{id}/grade`; Pydantic DTOs in `app/schemas/`; routers→services→repositories, domain errors→HTTP codes; in-process ASGI integration tests) + 1.5b done (remaining routers complete the full HTTP loop: `POST /discover` + `/discover/accept`, `POST /explain` caching into the `cards.word_explanations` JSONB column keyed by bare word—provider hit once, `GET/PUT /proficiency/{id}`, `GET/PUT /settings`; `tests/api/test_full_loop.py` walks Generate→Save→Review→grade→Discover over HTTP with a future `due`). 1.5 done. 1.6 done — `scripts/dump_openapi.py` dumps the canonical public schema (test-only routes excluded) to a checked-in `apps/api/openapi.json`, with `tests/test_openapi_stable.py` failing on drift vs `app.openapi()`; new `packages/api-types` pnpm-workspace package generates typed models + an `openapi-fetch` client from it (`apps/web` is now a workspace member on a single root `pnpm-lock.yaml`), and the `lint + format + types` CI job regenerates + drift-checks + typechecks it. 1.7 done — observability skeleton in `app/observability.py` (wired from `create_app()`): OpenTelemetry `TracerProvider` + auto-instrumentation for FastAPI/SQLAlchemy/httpx with a no-op exporter unless `OTEL_EXPORTER_OTLP_ENDPOINT` is set, plus a `RequestLoggingMiddleware` emitting one structured JSON access line per request (`method`/`path`/`status`/`latency_ms`) stamped with the active `trace_id` for log↔trace correlation (OTel deps shipped as prod deps so the Docker image is instrumented too). **Phase 1 exit gate: all 7 items ✅** (full HTTP loop, reversible Alembic schema, `lengua_core` purity + SQL-only-in-repositories, LLM config-flip, OpenAPI→TS-client no-drift, observability hooks live). M1 reached. |
| 2 | [phase-2-auth-multitenancy.md](phase-2-auth-multitenancy.md) | Supabase Auth, per-user scoping, RLS, account lifecycle | M | 1 | 28 | **in progress** — 2.3 done (backend JWT verification): `app/auth.py` verifies the Supabase access token (signature + `exp` + `aud`; HS256 secret or RS256/ES256 via JWKS) → typed `CurrentUser`; `app/deps.get_current_user` (401 on missing/invalid) + back-compat `current_user`→UUID, exercised by a JWT-protected `GET /me`; expired/forged/`alg:none` all rejected; strict CORS allowlist (`CORS_ALLOW_ORIGINS`). Ships a reusable test auth helper (`tests/auth_helpers.py`: mint Supabase-shaped JWTs + `authenticate_as`/`install_test_auth` overrides) for the rest of Phase 2. 2.4 done (per-user scoping): the Phase 1.3b repositories/services already thread `current_user.id` with no hard-coded id (the `grep` half of 2.4.1 is clean) — added `tests/test_repositories_scoping.py` (per-method scoping), `tests/test_routes_auth.py` (every non-`/health` route → 401 without a JWT; `/health` → 200), `tests/test_cross_tenant_app.py` (user B can neither read nor mutate user A over the real stack, via a new `multiuser_client` real-JWT fixture), and an expanded `GET /me` (identity + `plan` + per-language proficiency via new `MeService`/`ProfilesRepository`, scoped to the token's user) with `tests/test_me.py`; `openapi.json` + `packages/api-types` regenerated. 2.5 done (profiles & demo account): new Alembic revision `0002` reproduces the canonical `handle_new_user()` + `on_auth_user_created` trigger (profiles-on-first-login, `plan='free'`), guarded so `alembic upgrade head` still applies on a bare Postgres; `tests/test_profiles_bootstrap.py` proves the trigger bootstraps exactly one free profile per signup (Alembic-built + live, no duplicate on re-login). `tests/test_no_guest.py` asserts no anon token issuance (`enable_anonymous_sign_ins=false` in the repo-root `supabase/config.toml`) + every write route is 401 without a JWT + zero anon inserts. `tests/test_demo_seed.py` exercises the existing `scripts/seed_e2e.py` demo/reviewer account end-to-end: email-verified, seeded rows, and a **real password-grant login → `GET /review/due`** (genuine ES256/JWKS token) returning ≥1 due card. 2.1.1/2.1.4/2.2.3 done (Supabase Auth config): the canonical repo-root `supabase/config.toml` now requires email confirmation (`enable_confirmations=true`) with a password policy (`minimum_password_length=8` + `lower_upper_letters_digits`), a full `site_url`+`additional_redirect_urls` allow-list (local/staging/prod web + `capacitor://localhost` + the `app.lengua://` deep-link scheme), and branded `[auth.email.template.*]` HTML emails under `supabase/templates/` (confirm/recovery/magic-link); `tests/test_auth_config.py` asserts the config contents (unit) + the live signup-confirmation / redirect-allow-list / branded-delivered-email behavior (integration). Google OAuth is scaffolded-enabled (env-wired, inert until creds) + Apple scaffolded-disabled, both documented in `infra/supabase/oauth-setup.md`. 2.6 done (RLS — defense-in-depth; merged after a 3-lens adversarial review + grant-coverage hardening): new Alembic revision `0003` reproduces the canonical RLS (enable RLS + an owner policy on the seven user tables, `llm_budget` left global), guarded on `auth.uid()` so it no-ops on a bare Postgres; new `app/db/rls.py` makes each request's DB session assume the non-privileged `authenticated` role + set `request.jwt.claims` (re-applied per transaction via an `after_begin` listener), wired through `app/deps.get_db` (now `Depends(current_user)`) so RLS is actually enforced beneath the app-layer scoping while migrations/seeds keep their privileged connections; `tests/test_rls_session.py` / `test_rls.py` / `test_rls_coverage.py` / `db/test_rls_migration.py` prove per-session scoping, DB-level cross-tenant isolation (incl. forged-row rejection + superuser survival), RLS-on-every-`user_id`-table coverage, and the migration round-trip. Full backend gate green (334 tests, 100% cov; `app/db/rls.py` 100% line+branch). The 3-lens review found one medium gap — the real `authenticated`-role write path was exercised end-to-end only for `/languages` — closed before merge by a full real-RLS write round-trip (`/cards/save` + `/review/grade` + `/proficiency`/`/settings` upserts through the un-overridden `get_db`) and an explicit grant-coverage check (`has_table_privilege`/`has_sequence_privilege` on all 7 RLS tables + the 3 identity sequences). 2.7 done (historical data migration): `scripts/import_sqlite.py` imports the operator's legacy single-user `data/lengua.db` into the multi-tenant Postgres schema under one CLI-passed account UUID via a privileged (`postgres`) connection (RLS-exempt) — remapping old integer ids to the new identity ids parent→child, preserving `fsrs_state`/`due`/`saved`/proficiency scores, folding legacy `settings`→`user_settings`; idempotent via per-table natural keys with a `--dry-run` that rolls the whole import back (`tests/test_import_sqlite.py`, 7 tests: source-count match + card spot-check, dry-run writes nothing, double-import no duplicates); runbook + README documented. 2.8 done (account-lifecycle export + delete) — **3-lens adversarial review → approve; hardened then merged** (security-sensitive + destructive): new `app/routers/account.py` adds `GET /account/export` (a downloadable `AccountExport` JSON bundle — profile/languages/cards/reviews/proficiency/settings — assembled by `ExportService` via repository-only reads, scoped to `current_user`) and `DELETE /account` (hard-delete via `AccountDeletionService` calling the Supabase Auth Admin API with the service-role key; the `auth.users → profiles → domain` `on delete cascade` removes everything atomically — the single irreversible step runs last, so a failure deletes nothing → `502` retryable). 2.8.2 confirmed the cascade already exists in both the canonical + live Alembic schema (no migration); the dependent table is `llm_usage`. Cross-tenant guard is structural (no user-id parameter; id derived from the JWT). Backend gate green (366 tests, ~99.9% cov; the new account router/service/schema 100%), incl. live integration proof (real A+B users: A's `DELETE /account` removes A's auth row + all domain rows, B untouched, A's old token rejected by GoTrue) + a live double-`DELETE` idempotency test (both calls 204) + literal `curl /account/export` scoping. Post-review hardening: the Admin delete sends `should_soft_delete=false` **explicitly** so the cascade-firing hard delete can't be flipped by a GoTrue default; the RLS-bypassing service-role key is typed `pydantic.SecretStr` (masked in logs/reprs); runbook records the "never migrate prod Alembic-only" cascade invariant. `openapi.json` + `packages/api-types` regenerated. Remaining: 2.1.2/2.1.3 (owner Google/Apple OAuth creds) + 2.2.1/2.2.2 (owner Resend SMTP + SPF/DKIM/DMARC). |
| 3 | [phase-3-llm-quota.md](phase-3-llm-quota.md) | LLM quota, rate-limit, cost-guard kill-switch | M | 2 | 26 | **done (M2 reached)** — 3.1 done (usage accounting + server-only kill-switch privilege model): the `llm_usage`/`llm_budget` tables (PKs + `on delete cascade` FK) are confirmed from Phase 1; Alembic `0004` + a new canonical `supabase/migrations/20260626120000_llm_killswitch.sql` add the kill-switch privilege model — atomic `SECURITY DEFINER` `increment_llm_usage(uuid,text,date)` (both-counter row-locked bump, returns new budget) + `get_llm_budget_count(date)` reader owned by `postgres`, `REVOKE ALL ON llm_budget FROM authenticated, anon`, `GRANT EXECUTE` to `service_role` ONLY (not `authenticated` → PostgREST RPC can't trip the kill-switch); all role grants/revokes guarded on `to_regrole(...)` so the migration round-trips on bare Postgres. New `app/repositories/usage.py` (`UsageRepository`) + privileged `app.deps.get_usage_db` (RLS-bypassing, server-only). Tests prove the privilege model: 50-way concurrent atomicity (no lost updates), reads default 0, `llm_usage` cross-tenant isolation, `authenticated` denied SELECT/UPDATE on `llm_budget` + no EXECUTE on the functions, migration round-trip. Backend gate green (379 tests, 100% line+branch cov). **Merged PR #31.** 3.2 done (per-user daily caps): typed quota config in `app/settings.py` (`MAX_*_PER_DAY` 50/30/100 + `DEFAULT_*_PER_DAY` 20/10/50, env-overridable, documented in `.env.example`); new `app/quota.py` with `resolve_user_cap` (per-user `user_settings` override clamped by `min()` to the server max; missing/blank/non-numeric → default), the daily-cap gate (`enforce_daily_cap`/`QuotaGuard.check` → `DailyCapReached` mapped to **429** `{"code":"daily_cap_reached","kind":...}` by an app-level handler), and a single shared `QuotaGuard` applied to every LLM endpoint. `/generate`, `/discover`, `/discover/accept` (counted as `generate`) wire it as the `quota_guard(kind)` **dependency**; `/explain` is cache-aware so the SAME guard runs **inside `ExplainService` after the cache lookup** (cache hit free; only a miss is gated+counted). On-success increments go through `UsageRepository.increment_usage(current_user.id, kind, today-UTC)` on the privileged `get_usage_db` session (JWT-derived id only). `app/quota.py` is structured for the documented gate order (email-verified→rate-limit→daily-cap→global-budget) with extension points for 3.3/3.4. Backend gate green (388 tests, 100% line+branch cov; `app/quota.py` 100%). `openapi.json`+`packages/api-types` regenerated (route-description deltas only). 3.3 + 3.7 done (per-user rate limit + email-verified gate + signup-abuse guard): new `app/ratelimit.py` — a `RateLimiter` Protocol + an in-process sliding-window-log `InProcessRateLimiter` with an injectable clock (default `time.monotonic`), exposed via the process-wide-singleton `get_rate_limiter()` dependency (**locked decision: in-process, no slowapi**; distributed Postgres/Upstash swap flagged for Phase 6). `QuotaGuard.check` now runs the FULL chain in documented order **email-verified → rate-limit → daily-cap (→ global-budget slot)**: 3.7.1 email gate (`CurrentUser.email_verified` false → **403** `{"code":"email_unverified"}`, zero provider calls), 3.3.2 rate gate (over `RATE_LIMIT_PER_MIN` default 10 → **429** `{"code":"rate_limited"}` + `Retry-After`, keyed by `user_id` across all kinds, token consumed once past email), then the 3.2 daily cap. 3.7.2 signup-abuse day-0 clamp inside `enforce_daily_cap`: a brand-new account (its `profiles.created_at` is on the current UTC day, read via `ProfilesRepository`) gets effective generate cap = `min(resolved_cap, NEW_ACCOUNT_DAY0_GENERATE_CAP)` (default 5); established accounts use the normal cap; a code comment marks where a CAPTCHA would slot in (design-only). New env vars `RATE_LIMIT_PER_MIN`/`NEW_ACCOUNT_DAY0_GENERATE_CAP` documented in `.env.example`; README "Usage & cost limits" updated; routers/`openapi.json` unchanged (gates are app-level exceptions + a dependency). Tests: `tests/quota/{test_eligibility,test_ratelimit,test_gate_order,test_abuse_guard}.py` (+ `test_config` rate/day-0 load); api fixtures override `get_rate_limiter` with a fresh per-test limiter for isolation. Backend gate green (396 tests, **100.00%** line+branch cov; `app/quota.py` + `app/ratelimit.py` both 100%). 3.4 done (global daily budget kill-switch — **PR open, awaiting orchestrator review/merge**): config `GLOBAL_DAILY_BUDGET` (default 1000, documented in `.env.example` as "set below the active provider's free RPD"); the global-budget gate is the LAST in `QuotaGuard.check` — it reads the GLOBAL `llm_budget` counter on the **privileged** `get_usage_db`/`UsageSession` (the `authenticated` role is REVOKE'd from `llm_budget`) and, at/over the ceiling, refuses **every** caller with **429** `{"code":"daily_limit_reached","message":"Daily limit reached, please try again tomorrow."}` (`GlobalBudgetReached` → app-level handler). Status 429 chosen (03-backend.md lists "503/429" without mandating 503) to match the other quota gates. Increment stays check-then-increment-on-success (`record_success` runs only after a successful provider call, atomically bumping both `llm_usage`+`llm_budget`); failed/blocked/cache-hit calls burn no budget; bounded overshoot under concurrency documented at the gate (no refund path). Tests: `tests/quota/test_budget.py` (`test_kill_switch_trips`, `test_failed_call_no_increment`), `tests/integration/test_global_killswitch.py` (new `tests/integration/` dir — global, not per-user: A spends the budget, B is then refused too, zero real LLM calls), `tests/test_config.py::test_global_budget_loads`. Backend gate green (400 tests, **100.00%** line+branch; `app/quota.py` 100%); `openapi.json` unchanged (gate = app-level exception, no schema delta). 3.5 + 3.9 done (concurrency cap + backoff + BYOK key-resolution seam): new `app/llm_runner.py` — a process-global `asyncio.Semaphore` sized by `LLM_MAX_CONCURRENCY` (default 4) wraps **every** provider call; because the provider methods are sync/blocking, `LLMConcurrencyLimiter.run` offloads each to a worker thread (`asyncio.to_thread`) UNDER the semaphore (keeps the event loop responsive + bounds in-flight calls), exposed via the `get_llm_limiter()` singleton dependency (mirrors `get_rate_limiter`; overridable in tests, `reset_llm_limiter()` rebuilds) and threaded through the Generate/Discover/Explain services + routers. Over the cap a request waits briefly (bounded) then `ProviderBusy` → **503** `{"code":"server_busy",...}` (+ short `Retry-After`), never an unbounded queue, never a 500. 3.5.2: `lengua_core/llm/retry.py` `call_with_retry` gains **jitter** (injectable `rng`, full-jitter `base_delay*2**(n-1)*rng()`, deterministic under test) and on persistent 429/5xx raises a clean typed `LLMTransientError` (vendor error as `__cause__`) → same friendly **503** `server_busy` via an app-level handler (`register_llm_handlers`). 3.9.1: `resolve_llm_key(user)` in `lengua_core/llm/keys.py` is the single key chokepoint (operator key from env for the active provider; the `user` param is the inert future BYOK override) — both providers' `from_env` obtain the key ONLY through it, and a grep proves no other LLM-seam module reads the key env vars. 3.9.2: BYOK design note (`docs/byok-seam.md` + `keys.py` docstring) referencing the resolver + `profiles.plan` (key branch + cost-guard skip) — DESIGN ONLY, no storage/UI/columns. `LLM_MAX_CONCURRENCY` documented in `.env.example` (+ scale-out sequencing note); README "Usage & cost limits" extended (server-busy 503 + transient backoff). Backend gate green (416 tests, **100.00%** line+branch; `app/llm_runner.py` + `lengua_core/llm/{keys,retry}.py` all 100%); `openapi.json`+`packages/api-types` regenerated (route-description deltas only — the 503 is an app-level handler, no schema response delta). 3.6 done (cost minimization): (3.6.1) `/generate` now caps request size via **Pydantic validation** — `GenerateRequest.words` `max_length` = the env-overridable `Settings.max_words_per_request` (default reuses `lengua_core.llm.retry.MAX_WORDS_PER_REQUEST=30`), so an over-limit list is **rejected 422** at the boundary (hard reject, not silent truncation; `cap_words` stays a defensive provider-side floor) and `openapi.json`/`api-types` now carry `maxItems` on `words`; the existing `GENERATE_MAX_TOKENS` output cap is asserted to reach the vendor (Groq `max_tokens=`). (3.6.2) confirmed the `/explain` `word_explanations` cache hit is free end-to-end — zero provider calls AND no `llm_usage` increment on a repeat — with the authoritative `test_explain_cache.py` (reconciled with the 1.5b persistence + 3.2 cap tests, adds the `llm_usage`-unchanged dimension). (3.6.3, NEW) a repeated `/discover` for the same `(user, language, topic, count)` within `DISCOVER_REUSE_WINDOW_SECONDS` (new env setting, default 300) reuses the prior preview from a new in-process TTL cache (`app/discover_cache.py`: `DiscoverCache` Protocol + `InProcessDiscoverCache` with injectable clock + bounded TTL/size eviction, singleton `get_discover_cache()`/`reset_discover_cache()` — mirrors the rate-limiter seam); `/discover` is now cache-aware exactly like `/explain` (UNCHECKED guard `enforce=False`; `DiscoverService.suggest` runs `check`/`record_success` only on a cache miss), so a reuse HIT makes no provider call and no gate/increment. New `MAX_WORDS_PER_REQUEST`/`DISCOVER_REUSE_WINDOW_SECONDS` documented in `.env.example` (+ the Phase-6 distributed-swap caveat) and README "Usage & cost limits"; api/quota/multiuser fixtures override `get_discover_cache` with a fresh frozen-clock cache so the process-global cache never bleeds across tests; `test_quota_endpoints.py` discover calls varied by topic (identical repeats are now free). Backend gate green (429 tests, **100.00%** line+branch; `app/discover_cache.py` + the touched `app/services/discover.py`/`app/routers/discover.py`/`app/schemas/generate.py` all 100%); `openapi.json`+`packages/api-types` regenerated (`words.maxItems` + route-description deltas). 3.8 done + **Phase 3 EXIT GATE closed (M2 reached)**: per-LLM-call observability — every gated operation emits an OTel `llm.call` span (`llm.provider`/`model`/`latency_ms`/`tokens_in`/`tokens_out` set at the new `app/llm_runner.py` `run_provider` boundary from vendor token usage threaded out via a core seam `lengua_core/llm/usage.py`, plus `quota.kind`/`quota.cap_hit`/`budget.remaining` set by `QuotaGuard`; a blocked call records tokens 0 + the gate and still emits a span) and increments cost-guard metrics (counters `llm_calls_total{kind,result}` + `llm_cap_hits_total{gate}`, an `llm_budget_remaining` ObservableGauge) via a module-owned `MeterProvider` in `app/llm_observability.py` that is no-op/zero-egress unless an OTLP endpoint is set (mirrors the tracer). `quota_guard` is now a generator dependency so the span is always finalized (success/blocked/error). The **zero-paid-usage load test** (`tests/integration/test_load_cost_guard.py`, pytest-async) is the headline proof: a genuinely concurrent multi-user wave spends exactly `GLOBAL_DAILY_BUDGET` then every further request across all users is refused `daily_limit_reached`; per-user daily-cap + rate-limit bursts never exceed their ceilings; the concurrency cap bounds in-flight calls; `FakeLLM.call_count == GLOBAL_DAILY_BUDGET` (≤ ceiling, never more — the operator key can't be billed past the budget). All 8 exit-gate boxes ticked. Backend gate green (446 tests, **100.00%** line+branch; new `app/llm_observability.py`/`app/llm_runner.py`/`app/quota.py`/`lengua_core/llm/usage.py` all 100%); ruff+mypy --strict clean; `openapi.json` unchanged (spans/metrics are internal). Owner-confirmation items (real Groq free-tier RPD → `GLOBAL_DAILY_BUDGET`; per-user caps; Phase-6 distributed limiter/cache swap; single-instance deploy constraint) logged in `planning/outstanding-work.md` §9. **Phase 3 done.** |
| 4 | [phase-4-web-app.md](phase-4-web-app.md) | React web app (parity with Streamlit) | L | 1–3 | 44 | **done (M3 reached)** — 4.1 done (app shell & foundations): built on the Phase-0 scaffold — shadcn primitives (Card/Input/Dialog/Toast/Toaster + the existing Button, all coverage-excluded under `components/ui/**`); a persisted light/dark `ThemeProvider` (shadcn CSS-variable tokens, `system`/light/dark, localStorage, `ThemeToggle`); a real `react-router` v6 route tree split into an unauthenticated `AuthLayout` (`/login`, `/signup` stubs) and an authenticated `AppLayout` (header/sidebar/content with primary nav) wrapping lightweight per-screen stubs (Dashboard/Generate/Review/Discover/Languages/Settings/Account) + a 404; a configured TanStack Query `QueryClient` (`createQueryClient`: staleTime 30s, query retry 1, mutation retry 0, no refetch-on-focus) with `QueryClientProvider` + dev-only React Query Devtools (lazy, tree-shaken from prod); and a lazy auth-only `supabase-js` client (`getSupabaseClient`) plus a fail-fast env validator (`readEnv` over `VITE_API_BASE_URL`/`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, documented in `apps/web/.env.example`). Web gate green (35 vitest tests, 100% line+branch+func+stmt over product code; lint/format/typecheck/build clean; the env-less Playwright home smoke updated to the new shell and passing). 4.2 done (typed API client): a root `pnpm gen:api` convenience script regenerates `packages/api-types/src/schema.ts` from `apps/api/openapi.json` (no backend change → the existing CI drift check stays clean); `apps/web` now depends on the `api-types` workspace package (single root `pnpm-lock.yaml` updated), and a new `apps/web/src/lib/api-client.ts` wraps `createApiClient` into a lazy authed singleton — request middleware injects `Authorization: Bearer <token>` read fresh from the CURRENT Supabase session per call (never cached), and an `unwrap()` helper returns typed data on 2xx or throws a typed `ApiError {status,code,message,retryAfter,body}` parsed from body+headers (the exact shape the cost-guard states `email_unverified`/`rate_limited`/`daily_cap_reached`/`daily_limit_reached`/`server_busy` need; transport failures → `ApiError` status 0). Web gate green (49 vitest tests, 100% line+branch+func+stmt; `api-client.ts` 100%; `pnpm gen:api` no-diff; frozen-lockfile install clean). 4.3 done (auth screens & session handling + E2E harness rework — the linchpin group): signup (email+pw, client validation mirroring the server policy, "check your email" verify-notice), login (bad-credentials + unverified-email states with a resend CTA + forgot-password link), email-verification landing (`/auth/callback` → into the app or a resend CTA), password reset (request + reset-with-token via `resetPasswordForEmail`/`updateUser`, expired-link state), Google+Apple OAuth buttons on both screens (graceful degradation — owner-only creds, `VITE_OAUTH_PROVIDERS` toggle), session bootstrap + `useAuth()` context (`AuthProvider` reads the session + subscribes to changes; degrades to signed-out env-less), route guards (`RequireAuth` redirects to `/login` remembering the target; `RedirectIfAuthed` is what redirects post-login), central token refresh + 401-retry in `api-client.ts` (single-flight refresh-once → retry; refresh-fail → sign-out → cache reset + redirect — self-reviewed for races/loops/leaked tokens), and sign-out (header menu → SIGNED_OUT → `queryClient.clear()` + redirect). New auth seam `lib/auth.ts` (wraps supabase-js, builds redirect URLs, maps GoTrue errors) + `lib/auth-validation.ts`. **E2E harness reworked** (`.github/workflows/ci.yml` `e2e` job): now builds the web bundle wired to the ephemeral stack (`VITE_*` → local API + Supabase; the env-less `build`-job bundle stays for a11y/perf), runs the API container with `SUPABASE_JWT_SECRET` (verifies real tokens for future authed E2E), and runs Playwright auth specs (`E2E_STACK=1`) against it using the seeded demo account — zero-real-LLM guarantee preserved. Added redirect entry `http://127.0.0.1:4173/**` to `supabase/config.toml` for the CI preview origin. Web gate green (135 vitest tests, 100% line / 99% branch over product code; lint/format/typecheck/build clean); Playwright validated locally against a real Supabase stack (signup/login/signout/redirect all green). The 401→refresh→retry path is covered in vitest (note: not reliably reproducible in E2E). 4.4 done (language management & CEFR level UI): an `ActiveLanguageProvider` (mounted in `AppLayout`) loads `GET /languages` and tracks the active language, persisted per user in localStorage and reconciled against the fetched list (invalid/absent → first language; empty → none) — every language-scoped query is keyed by the active id so switching the header `LanguagePicker` refetches them; an add-language form on the `/languages` screen (name/code/vowelized + starting CEFR band — RECONCILED: `POST /languages` takes name/code/vowelized only, so a non-default band is applied via a follow-up `PUT /proficiency/{id}`, and direction is derived from code per 4.9, so there's no "direction" field) that invalidates the languages query + makes the new language active; a confirm-dialog remove (cascade warning) that gates `DELETE /languages/{id}`; and a sidebar `CefrPanel` showing band + progress-to-next-band (red/orange/blue/green-by-tier over a neutral track) from `GET /proficiency/{language_id}` with a manual band override (`PUT`) that invalidates the proficiency query. Typed data hooks in `lib/{languages,proficiency}.ts` over the api-client; pure CEFR helpers in `lib/cefr.ts`. Web gate green (194 vitest tests, 100% line / 99.35% branch over product code; lint/format/typecheck/build clean); Playwright `e2e/languages.spec.ts` (pick → add(B1) → switch-scopes-band → override(C1) → remove) validated against the real ephemeral stack (all 4 e2e specs green), zero real LLM calls. E2E-harness deltas (no backend code change): the `e2e` job's API container now gets `SUPABASE_JWKS_URL` (the local stack signs ES256 tokens → verify via JWKS, not the HS256 secret) + `CORS_ALLOW_ORIGINS` for the 4173 preview origin — the first authed-API E2E (4.3 only hit Supabase directly). 4.5 done (Generate screen): word-input form (textarea + live chips, non-empty + word-cap validation where the cap is read from the OpenAPI schema — a new `pnpm gen:api` step emits `packages/api-types/src/constants.ts` `schemaLimits.generateWordsMaxItems` from `GenerateRequest.words.maxItems`, drift-checked in CI, NOT hardcoded); `POST /generate` via the typed client with an explicit in-progress state; results grouped back into sentences (recognition+production re-paired) rendered with translation + used-word chips; per-sentence select-and-save (default all) → `POST /cards/save` sending ONLY the selected sentences' cards, success toast + saved confirmation, "Generate more"/"Start over" reset, and a review-cache invalidation so saved (immediately-due) cards are reviewable; first-class friendly cost-guard states — the dedicated **shared** `DailyLimitPanel` (`components/daily-limit-panel.tsx`, tied to the quota-429 shape via `isDailyLimitError`, reused by Discover 4.7 + verified-shared by 4.10.2) for `daily_cap_reached`/`daily_limit_reached` (words preserved), plus friendly inline `server_busy`/`rate_limited`/`email_unverified`/generic states classified by the shared `lib/llm-error.ts`. New `lib/generate.ts` (typed `useGenerate`/`useSaveCards` + pure `parseWords`/`groupSentences`/`cardsForSentences`) and a small shared `apiErrorMessage` helper on the api-client. Web gate green (248 vitest tests, 100% line / 99.49% branch over product code — new files 100/100; lint/format/typecheck/build clean); `e2e/generate.spec.ts` (generate→select-subset→save→reviewable + stubbed-429 daily-limit panel) added to the ephemeral-stack suite, zero real LLM calls. 4.6 done (Review screen — the FSRS loop): `GET /review/due` loads the active language's batch split into new vs. due (counts header + thin progress bar); the session walks a stable client-side snapshot one card at a time (grading does NOT refetch, so the queue can't reshuffle mid-review) — show the prompt (front), reveal the answer (recognition shows the translation; production renders the target sentence tap-a-word enabled), then rate Again/Hard/Good/Easy in the **LOCKED red/orange/blue/green** colours which `POST /review/{card_id}/grade` (FSRS 1–4) and advances; clean "all caught up" empty state + a "done for today / check for more" (refetch) completion state. Keyboard shortcuts (space/enter reveal, 1–4 rate; ignored with modifiers or while a form control is focused; double-submit guarded). Tap-a-word popover on production cards (`TappableSentence`): whitespace tokenization with a `bareWord` that mirrors the backend `lengua_core.cards.STRIP_CHARS` exactly (so a cached-explanation lookup matches; `¿/¡` are intentionally NOT stripped), `POST /explain` keyed by **word + language** (served instantly from the card's pre-generated note when present — no request), dismiss on re-tap/close-button/Escape/outside-pointer, RTL-anchored. New `lib/review.ts` (typed `useDueQuery`/`useGradeCard`/`useExplainWord` + pure `isProductionCard`/`RATINGS`/`ratingButtonClass`/`bareWord`/`segmentSentence`/`cardExplanation`) and `components/tappable-sentence.tsx`. Web gate green (301 vitest tests, **100% line / 99.58% branch / 100% func** over product code — `review.ts` 100/100/100, `Review.tsx` + `tappable-sentence.tsx` 100% line; lint/format/typecheck/build clean); `e2e/review.spec.ts` (counts header + reveal + mouse-grade + keyboard reveal/grade, and a walk-to-a-production-card tap-a-word popover) added to the ephemeral-stack suite, grading with "Again" so it never depletes the shared demo deck, zero real LLM calls. 4.7 done (Discover screen): a count (defaulting to the user's `discover_count` from `GET /settings`, clamped to the `DiscoverRequest.count` bounds read from the OpenAPI schema — a new `gen:api` constant `schemaLimits.discoverCount{Min,Max,Default}`, NOT hardcoded) + optional topic form → `POST /discover` previews new words → accept (one-shot in-memory handoff `lib/generate-handoff.ts` + `navigate('/generate')` feeds the words into the EXISTING Generate flow 4.5 — the generate UI is reused, not duplicated) or reroll (refetch + replace). Reuses the **shared** `DailyLimitPanel` for the quota 429 + a new shared `LlmErrorState` (extracted so Generate + Discover render identical friendly cost-guard states — daily-limit/transient/verify/generic — with no duplication; Generate refactored onto it). New `lib/{discover,settings,generate-handoff}.ts` + `components/llm-error-state.tsx`. Web gate green (353 vitest, 100% line/branch/func over new product code; lint/format/typecheck/build clean); `e2e/discover.spec.ts` added (real-FakeLLM preview + browser-stubbed reroll/accept/429, zero real LLM calls). 4.8 done (settings & account screens): a **Settings** screen edits the three per-user prefs (daily new-card limit, daily total limit, discover count) and saves via `PUT /settings` with per-field bounds validation — `discover_count` against the REAL schema bound (`schemaLimits.discoverCount{Min,Max}`), the two review limits against the legacy app's product bounds (new 1–100 / total 1–500, documented; the review batch still uses the `lengua_core` config defaults, so wiring those per-user keys into the due batch is a tracked backend gap); an **Account** screen shows the signed-in email + sign-out, exports the full data bundle (`GET /account/export` → client-side JSON file download via `downloadJson`), and hard-deletes the account behind a confirm-TYPED dialog (`components/delete-account-dialog.tsx`): the delete button stays disabled until the exact phrase `delete my account` is typed, confirming calls `DELETE /account` EXACTLY ONCE then signs out (Supabase session cleared + `queryClient.clear()`) and redirects to `/login`, with the 502-retryable partial-failure surfaced friendly and the dialog kept open. New `lib/account.ts` + Settings/Account data layer in `lib/settings.ts`. Web gate green (390 vitest, 100% line / 99.66% branch over product code — `account.ts`/`settings.ts`/`Settings.tsx`/`Account.tsx`/`delete-account-dialog.tsx` all 100/100; lint/format/typecheck/build clean); `e2e/{settings,account}.spec.ts` added (settings change→save→reload persists + bounds-block; account export-download + confirm-typed-delete→mocked-204→redirect-to-/login, the demo account never actually deleted), zero real LLM calls. **Merged (PR #45)** after a 3-lens manager review + post-delete-teardown hardening. **4.8b done (backend parity must-fix):** `GET /review/due` now honors the per-user `daily_new_limit`/`daily_total_limit` settings — the router reads them via `SettingsService` and passes them into `ReviewService.due_split`, with pure unit-tested `resolve_review_limit`/`resolve_review_limits` helpers falling back to the `lengua_core` config defaults on a missing/blank/invalid value; the false `app/schemas/settings.py` "quota gate reads these" docstring is corrected to document where each key is actually consumed; and the web Settings form gains cross-field `new ≤ total` validation (now that the limits drive the batch). Backend gate green (464 tests, 100% line+branch; ruff + mypy --strict clean); web gate green (`corepack pnpm --filter web verify` exit 0); `openapi.json`+`api-types` regenerated (only the `/review/due` route description changed). 4.9 done (RTL, diacritics & complex scripts): per-language direction derived from the code (`directionForCode` → `dir` on each screen's content region + on the target-text components; Arabic/Hebrew → rtl, else ltr), self-hosted diacritic-correct fonts (`@fontsource/noto-naskh-arabic` + `@fontsource/noto-sans-hebrew`, bundled by Vite — no runtime CDN, mobile-webview-safe — applied via `font-arabic`/`font-hebrew`), a device-persisted vowel-marks toggle (`VowelMarksProvider`/`useVowelMarks` + the self-gating `VowelMarksToggle`, shown only for vowelized languages; `stripDiacritics` removes harakat/nikkud from displayed glyphs while the tap-a-word lookup keeps the canonical marked word so cached explanations still match), and RTL-aware tap-a-word (whitespace `segmentSentence` is direction-agnostic + RTL-anchored popover; touch + click). New `lib/language-text.ts` + `components/{language-text,vowel-marks-context,vowel-marks-provider,vowel-marks-toggle}.tsx`; Generate/Review/Discover render target text via `LanguageText`/`TappableSentence`. A vowelized Hebrew (nikkud) deck added to `scripts/seed_e2e.py` (additive + idempotent; Spanish stays the primary `SeedResult`) powers `e2e/rtl.spec.ts` (dir=rtl + `document.fonts.check` + toggle strip/restore + mid-sentence touch/click tap). Web gate green (`corepack pnpm --filter web verify` exit 0; 446 vitest, 100% line / 99.68% branch over product code — all new files 100/100; fonts bundled in the build); backend seed change ruff+mypy --strict clean. 4.10 done (cross-cutting UX states + consent): shared `LoadingState`/`EmptyState`/`ErrorState` (over a `ui/skeleton` primitive) consolidate the per-screen loading/empty/error affordances and are wired into Generate/Review/Discover (the active-language context now exposes `refetch` so the languages-query error is retryable, and Review's due-batch loading/empty/error route through the same components); 4.10.2 formalized the single shared 429 `DailyLimitPanel` (a source-scan test proves Generate+Discover both consume it via `LlmErrorState`, no duplicate 429 UI); 4.10.3 added the first-run analytics-consent seam — `lib/analytics.ts` boots PostHog at most once and ONLY after explicit opt-in AND with `VITE_POSTHOG_KEY` set (default no-op so dev/CI/E2E load nothing; full PostHog wiring is Phase 5/8), via `AnalyticsConsentProvider`/`AnalyticsConsentBanner` mounted app-global in `main.tsx`, persisted so it never re-prompts. Also folded in a security-review nit: the `RequireAuth → /login → back` redirect now preserves the FULL target location (pathname+search+hash), not just pathname (`route-guards.tsx`). Web gate green (`corepack pnpm --filter web verify` exit 0; 477 vitest, 100% line/func, 99.7% branch over product code — all new files 100/100; the only sub-100 branches are the pre-existing `auth-provider`/`api-client` ones; `e2e/consent.spec.ts` added). 4.11 done (legacy-Streamlit retirement note): [`docs/streamlit-parity.md`](../../docs/streamlit-parity.md) maps every legacy surface — home (`app.py`) + the always-present sidebar (`ui.py`) + each `pages/*` page — to its reachable React screen/route/component, all `pages/*` features ✅ (the lone Gemini-model-selector is ♻️ intentionally retired, since the LLM provider/model is now operator/server config), with two documented ✅* nuances (existing-language `vowelized` flip not yet UI-wired — backend `PATCH` exists; Discover "accept" routes through the reused Generate flow by design); the README "Legacy Streamlit" table row + a new prose section are both marked **deprecated — retained for reference** (legacy code NOT deleted, stays runnable as a reference/fallback until prod (P6) + mobile (P7)); docs index updated. **All 44 Phase-4 task boxes done.** **Exit gate CLOSED — all 7 boxes ticked:** the milestone PR adds the dedicated end-to-end `apps/web/e2e/full-loop.spec.ts` (login → generate → save → review reveal+grade → discover in ONE session on the FakeLLM ephemeral stack, through the real server seam, zero real LLM calls) so a single spec proves the whole loop, and strengthens `e2e/auth.spec.ts` to assert BOTH Google + Apple buttons present+enabled; the auth-401→refresh→retry path stays vitest-covered (4.3 decision: not reliably reproducible in E2E), and parity / RTL / 429+consent / typed-client-drift are each proven by their group's merged green E2E + the CI `pnpm gen:api` drift check (re-confirmed on the merge commit). **Phase 4 complete → M3 (React web app at full parity with Streamlit) reached.** |
| 5 | [phase-5-observability.md](phase-5-observability.md) | Tracing, logs, metrics, dashboards, alerts | S–M | 1 (starts), 3, 6 | 32 | **CODE-COMPLETE & merged (PRs #51–#58)** — all 8 buildable groups landed green; **21/32 task boxes ticked** (every CI-verifiable half — in-memory OTel exporters, Playwright assertions, dashboard metric-name drift tests). The **11 open task boxes** (5.1.5, 5.3.3, 5.4.3, 5.6.4, 5.9.3, 5.7.1–5.7.5, 5.8.1) **+ all 7 exit-gate items are owner/Phase-6 LIVE verifies** — they need a deployed Cloud Run (Phase 6) + live Grafana Cloud / Sentry / PostHog creds, each recorded as-code-done/wiring-later in [outstanding-work.md §11](../outstanding-work.md). The cost-guard observability (5.2) + Sentry debug-endpoint (5.4) PRs were manager-reviewed clean (additive only — `test_load_cost_guard.py` stays green; debug routes prod-inert). **Phase 5's exit gate closes alongside the Phase-6 deploy.** 5.1 (OTel backend foundation): 5.1.1–5.1.4 done; 5.1.5 open (live/owner). 5.1.1 adds a shared `deployment.environment` resource (from `DEPLOYMENT_ENVIRONMENT`, defaulting to `ENV`; default `local`) applied to BOTH the `TracerProvider` and the cost-guard `MeterProvider` via the new `app.observability.build_resource` (deviation: no `opentelemetry-distro` — it auto-configures providers and would fight the existing manual SDK wiring). 5.1.2 verifies the single FastAPI server span per route (`GET /health` → method/route-template/`http.status_code=200`). **5.1.3 found + fixed a real gap:** the app engine was built via an early-bound `create_async_engine` (captured before instrumentation ran), so it emitted only the class-level `connect` span and NO statement spans — `app.db.session.get_engine` now resolves the OTel-wrapped factory **at call time** so DB `SELECT`/`INSERT` spans nest under the request span (offline guard + a `@pytest.mark.integration` test asserting a `SELECT` span is a descendant of the `GET /languages` server span). 5.1.4 verifies httpx CLIENT spans nest under the active span against a controlled loopback stub — RECONCILED: under `FakeLLM` (dev/CI/E2E) the provider call is in-process (no outbound httpx → the provider signal is the custom `llm.call` span from 3.8/5.2); the prod Groq/Gemini path produces the httpx client span. 5.1.5 left OPEN: as-code wiring done + CI-verified (env-driven `BatchSpanProcessor` attached only when the OTLP endpoint is set, none otherwise; `.env.example` documents the Grafana Cloud Tempo endpoint + the auth-token header) but the live "find the trace in Tempo within ~1 min" verify needs the Phase-6 staging deploy + Grafana creds (owner) — logged in `planning/outstanding-work.md` §11. Backend gate green (ruff + ruff format + mypy --strict clean; obs unit tests pass, integration auto-skip offline). NAMING: shipped provider-agnostic `llm.*` span + `llm_*` metric names are canonical (the plan's `gemini.*`/`gemini_*` predate the LLM seam); `llm_cap_hits_total{gate}` IS the quota-blocks counter — see §11. **5.2 done (custom spans & domain metrics — ADDITIVE; `test_load_cost_guard.py` stays green, zero guard-behavior change):** 5.2.1 extends the canonical `llm.call` span (NOT `gemini.*`) with `llm.input_size` (words/count/1 per kind) + `llm.retry_count` (threaded out of `call_with_retry` via the usage seam); 5.2.2 a `review.grade` span (rating/next_due/proficiency_delta); 5.2.3 a dedicated `quota.check` span (sibling of `llm.call`, emitted on admit AND block, carrying `user.cap_remaining` + `budget.remaining` — daily-cap state read ONCE via the new read-only `resolve_daily_cap_state`, no gate-decision/threshold/order change); 5.2.4 adds the token counter `llm_tokens_total{kind,direction}` (the plan's `quota_blocks_total{reason}` IS the shipped `llm_cap_hits_total{gate}`, gate==reason — no duplicate); 5.2.5 new `app/product_metrics.py` (one shared `MeterProvider`) emits `reviews_total`/`cards_created_total`/`signups_total` + an `active_users` observable gauge, wired at grade/save/`/me` (process-local; Phase-6 distributed caveat noted); 5.2.6 wires the app `MeterProvider` into `FastAPIInstrumentor` so the per-route `http.server.duration` RED histogram is emitted (CI-asserted via the in-memory reader; live per-route p95 in Mimir is owner/Phase-6, logged in §11). Backend gate green (ruff + ruff format + mypy --strict clean; new offline obs unit tests pass; integration auto-skip offline → CI authoritative). **5.3 (structured, correlated logging): 5.3.1 + 5.3.2 done; 5.3.3 open (live/owner/Phase-6).** 5.3.1 KEEPS the 1.7 stdout JSON access line (primary Cloud-Run→Loki path) and adds the direct-to-Loki alternative: a module-owned OTel `LoggerProvider` + `LoggingHandler` (root + access loggers) routing stdlib logging through OTel, with a `BatchLogRecordProcessor(OTLPLogExporter())` attached ONLY when `OTEL_EXPORTER_OTLP_LOGS_ENDPOINT`/`…_OTLP_ENDPOINT` is set (no-op/zero-egress otherwise, mirroring the tracer/meter; same `build_resource` so Loki filters by `service_name=lengua-api`). 5.3.2 adds `app.observability.TraceCorrelationFilter` (on the access stdout handler + the OTLP `LoggingHandler`) stamping `trace_id`/`span_id` (active span) + `user_id` (new `app.request_context` contextvar set in `app.deps.get_current_user` — additive, auth unchanged) onto every record; in-request logs carry the real `user_id` (the access line is emitted in the outer ASGI task so its `user_id` is `None` — documented). Tests in `tests/obs/{test_logging.py,test_log_correlation.py}`; backend gate green (ruff + ruff format + mypy --strict clean; new obs tests pass; new code 100% line+branch offline). NOTE: `opentelemetry.sdk._logs.LoggingHandler` is deprecation-flagged in OTel ≥1.43 (relocation; logs signal still unstable) — kept, revisit at stabilization (§11). 5.3.3 (Grafana Tempo→Loki trace-to-logs link) is entirely live + owner + Phase-6. **5.5 (frontend trace propagation) done — 5.5.1 + 5.5.2 (CI halves) ticked.** 5.5.1: minimal dependency-light W3C `traceparent` propagation (NOT a heavy `@opentelemetry` browser SDK) — new `apps/web/src/lib/trace.ts` `generateTraceparent()` (version `00`, sampled `01`, `crypto.getRandomValues` 16-byte trace-id + 8-byte span-id, never all-zero) injected by a `traceMiddleware` in the openapi-fetch `onRequest` (`apps/web/src/lib/api-client.ts`); the 401 refresh/retry clones the request so the header is preserved (vitest-proven), and `e2e/generate.spec.ts` asserts a real API request carries an outgoing W3C-shaped `traceparent`. 5.5.2: `apps/api/tests/obs/test_trace_continuation.py` (`@integration`) proves a supplied client `traceparent` CONTINUES into the FastAPI server span (same trace-id, parented under the client span-id — OTel default W3C `tracecontext` propagator) and that the SQLAlchemy + `llm.call` child spans share that trace-id → client→API→DB/LLM is ONE trace (provider signal under `FakeLLM` is the `llm.call` span, not an httpx/`gemini.generate` span — naming reconciliation §11). Web gate green (484 vitest, 100% line/func, `trace.ts` 100/100; lint/format/types/build clean); backend obs file ruff+format+mypy --strict clean; the live Tempo-assembly + browser-span-export-to-Tempo halves are owner/Phase-6 (logged §11). **5.4 (error tracking — Sentry): 5.4.1 + 5.4.2 done (CI halves); 5.4.3 open (live/owner).** 5.4.1 (backend) — new `app/error_tracking.py` runs `sentry_sdk.init` ONLY when `SENTRY_DSN_API` is set (no-op/zero-egress otherwise, mirroring the OTLP discipline) with the Starlette+FastAPI integrations, `environment` from the shared `deployment_environment`, and `send_default_pii=False`; `bind_request_scope` (called from `get_current_user`, additive — no auth-decision change) stamps the authenticated `user.id` + a `trace_id` tag (active OTel span) onto Sentry's per-request **isolation scope** (robust across the `BaseHTTPMiddleware` task boundary, unlike a contextvar) so an auto-captured exception carries both; a deliberately-failing `GET /__test__/debug-error` is gated EXACTLY like `/__test__/llm-calls` (only under `LLM_PROVIDER=fake`) AND requires a valid bearer token → prod-inert, anonymous-unreachable, generic-500 (no leak). `tests/obs/test_sentry.py` proves the no-DSN no-op + the enabled init + the end-to-end capture carrying `user_id` + a 32-hex `trace_id` via an in-memory transport (zero egress). 5.4.2 (web) — `@sentry/react` + `apps/web/src/lib/error-tracking.ts`: `initErrorTracking` runs `Sentry.init` ONLY when a build-time `VITE_SENTRY_DSN_WEB` is set (no-op otherwise — dev/CI/E2E load nothing) with `browserTracingIntegration()` (perf/Web-Vitals) + `sendDefaultPii:false`. **RECONCILED:** introduced `VITE_SENTRY_DSN_WEB` (the old `SENTRY_DSN_WEB` wasn't `VITE_`-prefixed → couldn't reach the bundle), added to `apps/web/.env.example` + `vite-env.d.ts` (+ root `.env.example` pointer). A hidden sr-only `DebugErrorButton` (app-global in `main.tsx`) renders only when `VITE_ENABLE_DEBUG_TOOLS` is set (a flag prod NEVER sets → prod-inert) and on click routes a thrown error through the capture chokepoint; vitest covers init gating/options + capture path (new files 100%), `e2e/sentry.spec.ts` asserts the capture fired (recorded on `window`) AND zero Sentry egress (no DSN in the preview build), with the e2e build + local Playwright `webServer` setting `VITE_ENABLE_DEBUG_TOOLS=1`. Backend gate green (354 tests; ruff+format+mypy --strict clean; `app/error_tracking.py` + the debug route 100% line+branch); web gate green (`corepack pnpm run verify` exit 0; sentry+consent Playwright specs validated locally). 5.4.3 (Sentry alert rule → channel) is entirely live + owner; the live halves of 5.4.1/5.4.2 (issue→Tempo trace; web error/Web-Vitals in the Sentry dashboard) need the Phase-6 deploy + live Sentry/Grafana creds — all logged in `planning/outstanding-work.md` §11. **5.6 (Grafana dashboards): 5.6.1–5.6.3 done (CI halves); 5.6.4 open (Phase-6).** Dashboards committed AS CODE under `infra/grafana/dashboards/` (portable via a `${datasource}` template var) referencing the REAL emitted names (§11): `service-health.json` (RED — per-route rate/5xx/p50-p95-p99 from the `http.server.duration` histogram), `cost-guard.json` (the "stay free" board — calls/day vs ceiling, `llm_budget_remaining`, `llm_cap_hits_total{gate}` blocks, `llm_tokens_total` — NOT the stale `gemini_*` names), `product.json` (`reviews_total`/`cards_created_total`/`signups_total`/`active_users`; CEFR distribution is a documented placeholder text panel pending a `proficiency_cefr_band` metric). The CI-verifiable lint/drift test `apps/api/tests/obs/test_dashboards.py` proves each dashboard is valid Grafana JSON (unique uids/panel-ids, declared datasource vars) AND that every PromQL metric maps to an instrument the backend actually emits — cross-referenced via AST of `app/llm_observability.py`+`app/product_metrics.py` + the FastAPI RED histogram, so a metric rename without a dashboard update fails CI. `infra/grafana/README.md` documents import/provisioning + the metric-name policy (incl. the one env-specific knob: the histogram's `_milliseconds` unit suffix). The live "renders non-empty after a load script" half is owner/Phase-6; **5.6.4 (Infra) left OPEN** — entirely depends on the Phase-6 Cloud Run deploy (no Cloud Run metrics exist yet); a tagged `phase6-skeleton` JSON is committed (excluded from the cross-ref) so it's ready to wire. Backend gate green (ruff + ruff format + mypy --strict clean; the 15-case dashboard test passes; no `app/` code touched so coverage unaffected). Logged in §11. **5.9 (product analytics — PostHog): 5.9.1 + 5.9.2 done (CI halves); 5.9.3 open (live/owner).** 5.9.1: the real PostHog adapter (`apps/web/src/lib/posthog.ts`, registered in `main.tsx`) lazily `import('posthog-js')`s (code-split, NOT in the main chunk) pointed at the EU host `https://eu.i.posthog.com` with autocapture/pageview/session-recording OFF + `person_profiles:'identified_only'` (anonymized); capture stays gated on consent (the 4.10.3 seam, now extended with `captureAnalytics`/`applyAnalyticsConsent` + capturer/consent-applier injection seams) AND `VITE_POSTHOG_KEY`, so dev/CI/E2E load + send NOTHING (the existing `e2e/consent.spec.ts` stays green). A consent TOGGLE Card on Settings (`components/analytics-consent-toggle.tsx`) flips consent post-banner — grant → boot/`opt_in`, deny → `posthog.opt_out_capturing()`. The keyed path is CI-proven by `lib/posthog.test.ts` (mocks posthog-js: `posthog.init(key,{api_host:eu,…})` + opt-out/opt-in). 5.9.2: `lib/analytics-events.ts` fires the four named funnel events `signup`/`language_added`/`generate`/`review` through the consent-gated capture at Signup success + `useAddLanguage`/`useGenerate`/`useGradeCard` `onSuccess`, with NO PII (`{method}`/`{code}`/`{word_count}`/`{rating}` only — never email/name/the words/sentences); `lib/analytics-events.test.ts` asserts all four fire with no PII (denylist) + consent-gating, and each integration-point test asserts fire-on-success-only. Web gate green (`corepack pnpm run verify` exit 0; 527 vitest, 100% line/func, 99.72% branch over product code — all new files `analytics.ts`/`posthog.ts`/`analytics-events.ts`/`analytics-consent-toggle.tsx` 100/100; the only sub-100 branch is the pre-existing `api-client.ts`; lint/format/types/build clean; posthog-js code-split into its own chunk). Live PostHog ingestion (events reach the EU project) is owner; 5.9.3 (funnel/retention/feature-usage insights) is entirely a live PostHog-dashboard task — both logged in §11. **5.7 + 5.8 (alerts + external uptime) — as-code COMMITTED, NOTHING ticked (every verify is LIVE delivery / staging-traffic / external-prober, owner + Phase-6).** Committed Grafana provisioned-alerting under `infra/grafana/alerts/`: a `lengua-oncall` contact point (Slack primary; email/Discord alternatives, secrets as env-expanded placeholders) + notification-policy tree (5.7.1), and four alert rules over the REAL emitted metrics (5.7.2 API 5xx ratio >5%/5m + 5.7.3 overall p95 >1.5s/10m over the `http_server_duration_milliseconds` histogram; 5.7.4 `llm_budget_remaining` <20% of `GLOBAL_DAILY_BUDGET` — the early warning before the kill-switch, NOT the stale `gemini_*` name; 5.7.5 prod `/health` `probe_success` down). External uptime monitor as-code under `infra/uptime/` (5.8.1 — vendor-neutral `GET /health` descriptor + BetterStack/UptimeRobot/Grafana-Synthetic encodings). `docs/runbook.md` "Health checks" filled in (the `/health` probe, Tempo/Loki/dashboards/alerts signals, green-vs-red; Deploy-rollback→Phase 6, On-call→Phase 9). CI-lint test `apps/api/tests/obs/test_alerts.py` (parses + condition-refId integrity + uid uniqueness + PromQL metric cross-ref against emitted instruments + the budget-threshold-tracks-`GLOBAL_DAILY_BUDGET` drift guard) green; ruff+format+mypy --strict clean; no `app/` code touched (PyYAML added as a dev dep for the lint test). 5.7.1–5.7.5 + 5.8.1 logged in §11 as owner/phase6 live-deferred. Phase 5 remaining: 5.6.4 (Phase-6 infra dashboard), 5.9.3 (live PostHog insights), 5.4.3 + the live/owner verify halves (5.7.x/5.8.1 delivery, dashboards render, traces/logs/Sentry-issue in Grafana). |
| 6 | [phase-6-infra-cicd.md](phase-6-infra-cicd.md) | Environments, CI gate, CD pipeline, rollback | M | 1, 2 | 48 | **in progress** — 6.1 (Dockerize + probes) started: **6.1.1** confirmed + hardened the production multi-stage `apps/api/Dockerfile` (non-root, `$PORT`-honoring shell-form entrypoint) — base image now **pinned by sha256 digest** (multi-arch index) in both stages, and the CI `build` job gained a run-path **smoke step** (`docker run -e PORT=8080 … && curl /health` → 200) so the build+run verify is CI-enforced; **6.1.2** added the unauthenticated **`GET /ready`** readiness probe (a plain RLS-free-engine `SELECT 1` via `app.db.session.check_db_ready`, `200 {"status":"ready"}` / `503 {"status":"not_ready"}`, never 500; `/health` kept pure) with `tests/test_health_ready.py` (health always 200; /ready 503 when DB unreachable via stub + 200 via stub + an `@integration` real-DB 200) at 100% line+branch on the new code; `openapi.json`+`api-types` regenerated (adds `/ready` to the public schema like `/health`); route-auth test recognises `/ready` as a public infra probe. 6.10 (Domains & CORS) started: **6.10.1** (per-env CORS hardening) done — CORS is env-driven (`CORS_ALLOW_ORIGINS`) and `tests/test_cors.py` now asserts the configured allowlist + the actually-installed `CORSMiddleware` carry no `*` wildcard (incl. a prod-shaped config + the credentials-with-wildcard regression guard) and reject an un-listed origin, with `.env.example` documenting the exact-origins/no-wildcard prod rule. 6.3 (Vercel web) started: **6.3.4** (web-bundle secret-leak audit) done — the bundle is audited two ways: a CI `build`-job grep over the built `dist/` (forbidden env names + `gsk_`/`AIza`/`service_role` key shapes; validated zero-false-positive on the clean bundle, catches synthetic leaks, ignores the anon key) and `apps/web/src/lib/bundle-safety.test.ts` source-scan that locks the `VITE_*` surface to the client-safe set. 6.8 (Rollback & runbook): **6.8.3** done — `docs/runbook.md` now has concrete-command **Deploy / Rollback / Run-a-migration / Rotate-a-secret / Budget-exhausted-alert / Restore-from-backup / Store-release** sections (the live "follow rollback end-to-end on staging" confirmation is owner-deferred). 6.4 (secrets): **6.4.4** secret-rotation procedure documented in the runbook (Groq key / Supabase JWT secret + zero-downtime JWKS alternative / deploy credential + WIF) — box left unticked (live rotation owner-deferred). 6.2 (Supabase) reconciled as-code: **6.2.4** `infra/supabase/README.md` documents that the canonical RLS/trigger/kill-switch SQL stays at `supabase/migrations/` (NOT relocated — moving it would break the CLI stack), its Alembic-DDL relationship, and the per-project apply path; **6.2.5** documents the idempotent per-env seeding (`seed_e2e.py`/`seed_dev_user.py` via `DATABASE_URL`) — both left unticked (live staging apply/seed owner-deferred). 6.5 (CI per-PR gate) **CONFIRMED-AND-TICKED**: **6.5.1–6.5.8** — the blocking gate built in Phase 0 (`setup` · `lint-and-types` · `backend-tests` · `frontend-tests` · `build` · `e2e` · `security` · `coverage-comment` · `a11y-perf`) already satisfies every 6.5.x verify (the four E2E critical journeys signup / generate→save / review-four-ratings / 429-path; the OpenAPI→typed-client contract-drift chain `test_openapi_stable.py` + the api-types `git diff --exit-code`; gitleaks full-history via the security job's `fetch-depth: 0`) — reconciled purely additively, **no gate weakening**. 6.9 (Feature flags) **DONE as-code + ticked**: **6.9.1/6.9.2/6.9.3** — a typed `FeatureFlags` accessor (`app/feature_flags.py`) resolving each known flag from an env default overlaid by the new GLOBAL `feature_flags` table (Alembic 0005 + canonical Supabase SQL; REVOKE from `authenticated`/`anon` + deny-by-default RLS like `llm_budget` — writes admin/service-role only), cached for `FEATURE_FLAG_TTL_SECONDS` behind an injected clock so a table toggle takes effect within the TTL **with no redeploy** (deterministic refresh test); public `GET /feature-flags` + the web hook expose the PUBLIC map; the experimental `word_of_the_day` surface (API route + Dashboard card) ships dark (off by default, 404/absent until flipped on). Only the LIVE prod-without-redeploy confirmation is owner-deferred (§12). 6.6 + 6.7 + 6.8 (CD pipeline) **COMMITTED AS-CODE + GATED**: `.github/workflows/deploy-staging.yml` (merge→staging: build-push image → discrete `migrate-staging` → Cloud Run deploy w/ probes+scale-to-zero+max-instances=1 → Vercel staging → smoke) and `deploy-prod.yml` (gated `production`-environment approval → resolve staging-validated **image digest** → `migrate-prod` → promote → Vercel prod → smoke), sharing composite actions `.github/actions/cloud-run-deploy` + `cloud-run-smoke`, plus the one-click `infra/deploy/rollback.sh` (6.8.2). **Every CD job is gated `if: vars.DEPLOY_ENABLED == 'true'` (UNSET today → every job SKIPS → main stays green, zero live steps); the workflows never trigger on `pull_request`.** **6.6.3 mechanism TICKED** — the `alembic -x env=staging|prod|local` resolver (`app/db/migration_url.py`, maps `STAGING_/PROD_/DATABASE_URL`; `-x db_url=` still wins) + its precedence unit test (`tests/db/test_migration_url.py`, 100% line+branch) are CI-green. All other 6.6.x/6.7.x/6.8.1/6.8.2 boxes left `[ ]` (live Cloud Run/Vercel/Supabase verify) with as-code-done notes. Remaining 6.1.3–6.4 (as-code remainder) + 6.10.2/6.10.3 + the live cloud provisioning (Cloud Run/Supabase/Vercel/secrets, real prod origin values) + the owner `DEPLOY_ENABLED=true` flip + the `production`-environment reviewer + branch-protection enforcement of the gate (0.6.3) are owner/live-deferred — see [outstanding-work.md §12](../outstanding-work.md). |
| 7 | [phase-7-mobile.md](phase-7-mobile.md) | Capacitor → signed iOS + Android, OTA | M | 4, 6 | 50 | not started |
| 8 | [phase-8-compliance-store.md](phase-8-compliance-store.md) | Privacy/GDPR, store labels, listings, closed tests | M | 7 | 27 | not started |
| 9 | [phase-9-launch.md](phase-9-launch.md) | Coordinated web + iOS + Android launch + 48h watch | S | 0–8 | 15 | not started |

**Total: 349 PR-sized tasks across 10 phases.**

---

## Dependency graph & critical path

```
            ┌──────────────────────── cross-cutting ────────────────────────┐
            │  Observability (P5): instrument from P1 ──► finish in P5       │
            │  Infra/CI (P6): CI gate from P0 ──► deploy pipeline in P6      │
            └───────────────────────────────────────────────────────────────┘

  0 ──► 1 ──► 2 ──► 3 ──► 4 (web) ──► 7 (mobile) ──► 8 (compliance) ──► 9 (launch)
        │         │      │             ▲                  ╲                 ▲
        │         │      │             │                   ╲                │
        │         │      │             └── 6 (infra/CI) ────┘  8 overlaps 7 │
        │         │      │                  runs alongside 4–6              │
        │         │      └────────────────────────────────────────────────┐│
        │         └── 5 (observability) starts here, runs alongside 1–6 ───┘│
        │                                                                    │
        └── 9 needs web (P4) + iOS + Android (P7, via P8) all green ─────────┘
```

Edges in words:

- **0 → 1 → 2 → 3** — the backend spine (foundations, core loop, auth/multi-tenancy, cost guard).
- **4 (web)** follows **3**; **7 (mobile)** wraps the **4** web build; **8 (compliance)**
  overlaps **7**; **9 (launch)** needs **web + iOS + Android** all ready.
- **5 (observability)** *starts* in Phase 1 (auto-instrument as the backend is built) and
  runs alongside **1–6**; a couple of its tasks (infra dashboard, external uptime) land
  after the matching **6** deploy.
- **6 (infra/CI)** runs alongside **4–6** (CI gate already seeded in Phase 0).

**Critical path (explicit):**

```
0 → 1 → 2 → 3 → 4 → 7 → 8 → 9
```

---

## Cross-cutting workstreams

These run *across* phases rather than as a single block — track them continuously.

- **Observability** — instrument from **P1** (OTel auto-instrumentation + structured logs
  wired as the backend is built; custom quota spans land with **P3**), then *finish* in
  **P5** (dashboards, alerts, Sentry, external uptime). See
  [phase-5-observability.md](phase-5-observability.md).
- **Infra / CI** — the blocking **CI gate is seeded in P0** (`phase-0` group 0.5) and the
  full **deploy pipeline (CD) lands in P6** (build → Cloud Run staging → gated prod →
  rollback). See [phase-6-infra-cicd.md](phase-6-infra-cicd.md).
- **Testing & quality gate** — applies to **every PR in every phase**: 100% green + ≥80%
  backend & frontend coverage + Playwright E2E with the LLM stubbed. A task is not done
  until its tests keep coverage ≥80%. See [../09-testing-quality.md](../09-testing-quality.md).
- **Security & compliance** — threaded through **P2** (JWT/RLS/CORS, account
  export+delete), **P3** (shared-key abuse guard), **P7** (signing material as secrets,
  no leaked keys), and **P8** (privacy policy, GDPR consent/residency, store data-safety,
  deletion cascade). See [../07-security-compliance.md](../07-security-compliance.md).

---

## Setup readiness (pre-implementation)

A readiness validation on **2026-06-25** verified **~47 account/infra items done** (GitHub
repo + 28 Actions secrets, both Supabase projects, GCP `lengua-prod` + Artifact Registry +
`github-ci` SA, Vercel project, Groq/Gemini/Resend/Grafana/Sentry keys as CI secrets,
Ben's local `.env`/gcloud/Docker). Owner (Kotlar) item status (updated 2026-06-25):

1. Branch protection on `main` — **DEFERRED to launch** (non-blocking; would break the
   autonomous self-merge flow if enabled now). See [owner-deferred-tasks.md](owner-deferred-tasks.md).
2. Dependabot vulnerability alerts + automated security fixes — **DEFERRED to launch**
   (non-blocking). See [owner-deferred-tasks.md](owner-deferred-tasks.md).
3. Two CI secrets (`GCP_REGION`, `SENTRY_ORG`) — **still outstanding** (needed in Phase 5/6, not
   Phase 0).
4. Vercel access for Ben — **RESOLVED**: free-tier single manager seat; Ben is the account
   holder/manager for `lengua`. Non-blocking.
5. Resend custom-SMTP delivery confirmed in both Supabase projects — **still outstanding**
   (needed for Phase 2 auth emails).
6. Grafana Cloud + Sentry access for Ben — **DONE 2026-06-25** (Ben joined both).

So only items **3** (deploy/observability secrets) and **5** (auth email) remain truly open, and
**1–2** are deferred-by-design to launch — see [owner-deferred-tasks.md](owner-deferred-tasks.md).
**None of these block writing code.** **Paid store accounts** (Apple Developer $99/yr, Google
Play $25 one-time) are **deferred to Phase 7** and are not part of Phase 0.

---

## Milestones

| Milestone | What it proves | Lands at |
|-----------|----------------|----------|
| **M1** ✅ | Backend Generate→Save→Review→Discover loop runs over HTTP | end of **P1** |
| **M2** ✅ | Multi-user (auth + RLS) with the LLM cost guard armed | end of **P3** |
| **M3** ✅ | React web app at full parity with the legacy Streamlit app | end of **P4** |
| **M4** | Deployed to staging **and** prod (auto-staging + gated prod) | end of **P6** |
| **M5** | Signed iOS + Android builds installable on real devices | end of **P7** |
| **M6** | Coordinated web + iOS + Android launch (v1 live) | **P9** |
